`.conf.ini` -> `.conf` => enable

`.conf` -> `.conf.[xxyy]` (e.g., .conf.txt) => disable


Changing `.conf.ini` to `.conf` enables the configuration, changing `.conf` to another extension disables it.
